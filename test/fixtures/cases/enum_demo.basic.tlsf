INFO {
  TITLE:       "e";
  DESCRIPTION: "e";
  SEMANTICS:   Mealy;
  TARGET:      Mealy;
}

MAIN {

  INPUTS {
    go;
  }

  OUTPUTS {
    st[0..1];
  }

    ASSERT {
      (((st == A) || (st == B)) || (st == C));
    }

    GUARANTEE {
      G ((go -> (st == A)));
      G (((st == B) -> go));
      G (!((st == C)));
    }

}
