diff --git a/crates/oxidd-manager-index/src/manager.rs b/crates/oxidd-manager-index/src/manager.rs
index 1f82f5c..64060fd 100644
--- a/crates/oxidd-manager-index/src/manager.rs
+++ b/crates/oxidd-manager-index/src/manager.rs
@@ -22,8 +22,8 @@ use std::marker::PhantomData;
 use std::mem::ManuallyDrop;
 use std::ops::Range;
 use std::sync::Arc;
-use std::sync::atomic::AtomicU64;
-use std::sync::atomic::Ordering::{Acquire, Relaxed};
+use std::sync::atomic::{AtomicU64, AtomicUsize};
+use std::sync::atomic::Ordering::{AcqRel, Acquire, Relaxed};
 
 use crossbeam_utils::CachePadded;
 use derive_where::derive_where;
@@ -131,6 +131,9 @@ where
     terminal_manager: TM,
     state: CachePadded<Mutex<SharedStoreState>>,
     gc_signal: (Mutex<GCSignal>, Condvar),
+    gc_thread: Mutex<Option<std::thread::JoinHandle<()>>>,
+    /// Counts manager references without the GC thread's own Store Arc.
+    external_refs: AtomicUsize,
     workers: crate::workers::Workers,
 }
 
@@ -2033,7 +2036,6 @@ where
 // === ManagerRef ==============================================================
 
 #[repr(transparent)]
-#[derive_where(Clone)]
 pub struct ManagerRef<
     NC: InnerNodeCons<ET>,
     ET: Tag,
@@ -2055,6 +2057,21 @@ pub struct ManagerRef<
     >,
 );
 
+impl<
+    NC: InnerNodeCons<ET>,
+    ET: Tag,
+    TMC: TerminalManagerCons<NC, ET, TERMINALS>,
+    RC: DiagramRulesCons<NC, ET, TMC, MDC, TERMINALS>,
+    MDC: ManagerDataCons<NC, ET, TMC, RC, TERMINALS>,
+    const TERMINALS: usize,
+> Clone for ManagerRef<NC, ET, TMC, RC, MDC, TERMINALS>
+{
+    fn clone(&self) -> Self {
+        self.0.external_refs.fetch_add(1, Relaxed);
+        Self(self.0.clone())
+    }
+}
+
 impl<
     NC: InnerNodeCons<ET>,
     ET: Tag,
@@ -2065,12 +2082,18 @@ impl<
 > Drop for ManagerRef<NC, ET, TMC, RC, MDC, TERMINALS>
 {
     fn drop(&mut self) {
-        if Arc::strong_count(&self.0) == 2 {
-            // This is the second last reference. The last reference belongs to
-            // the gc thread. Terminate it.
+        if self.0.external_refs.fetch_sub(1, AcqRel) == 1 {
             let gc_signal = &self.0.gc_signal;
             *gc_signal.0.lock() = GCSignal::Quit;
             gc_signal.1.notify_one();
+            // Release the mutex before joining; GC can drop a transient
+            // ManagerRef as it exits.
+            let thread = self.0.gc_thread.lock().take();
+            if let Some(thread) = thread {
+                if thread.thread().id() != std::thread::current().id() {
+                    thread.join().expect("OxiDD GC thread panicked");
+                }
+            }
         }
     }
 }
@@ -2200,6 +2223,7 @@ impl<
         //   `&Manager` reference, the counter is at least 1.
         ManagerRef(unsafe {
             let manager = &*ptr;
+            (*manager.store).external_refs.fetch_add(1, Relaxed);
             Arc::increment_strong_count(manager.store);
             Arc::from_raw(manager.store)
         })
@@ -2304,6 +2328,8 @@ pub fn new_manager<
         }),
         terminal_manager: TMC::T::<'static>::with_capacity(terminal_node_capacity),
         gc_signal: (Mutex::new(GCSignal::RunGc), Condvar::new()),
+        gc_thread: Mutex::new(None),
+        external_refs: AtomicUsize::new(1),
         workers: crate::workers::Workers::new(threads),
     });
 
@@ -2317,17 +2343,20 @@ pub fn new_manager<
         LOCAL_STORE_STATE.with(|state| state.current_store.set(store_addr))
     });
 
-    // spell-checker:ignore mref
-    let gc_mref: ManagerRef<NC, ET, TMC, RC, MDC, TERMINALS> = ManagerRef(arc.clone());
-    std::thread::Builder::new()
+    let gc_store = arc.clone();
+    let gc_thread = std::thread::Builder::new()
         .name("oxidd mi gc".to_string())
         .spawn(move || {
             // The worker is dedicated to this store.
             LOCAL_STORE_STATE.with(|state| state.current_store.set(store_addr));
 
-            let store = &*gc_mref.0;
+            let store = &*gc_store;
             loop {
                 let mut lock = store.gc_signal.0.lock();
+                // Quit may have arrived before this thread first waited.
+                if *lock == GCSignal::Quit {
+                    break;
+                }
                 store.gc_signal.1.wait(&mut lock);
                 if *lock == GCSignal::Quit {
                     break;
@@ -2335,9 +2364,9 @@ pub fn new_manager<
                 drop(lock);
 
                 // parking_lot `Condvar`s have no spurious wakeups -> run gc now
-                oxidd_core::ManagerRef::with_manager_shared(&gc_mref, |manager| {
-                    oxidd_core::Manager::gc(manager);
-                });
+                let local_guard = store.prepare_local_state();
+                oxidd_core::Manager::gc(&*store.manager.shared());
+                drop(local_guard);
 
                 let mut shared = store.state.lock();
                 LOCAL_STORE_STATE.with(|local| {
@@ -2354,6 +2383,7 @@ pub fn new_manager<
             }
         })
         .unwrap();
+    *arc.gc_thread.lock() = Some(gc_thread);
 
     // initialize the manager data
     let local_guard = arc.prepare_local_state();
@@ -2498,6 +2528,7 @@ unsafe impl<
         //   `&Manager` reference, the counter is at least 1.
         let store = ManagerRef(unsafe {
             let manager = &*ptr;
+            (*manager.store).external_refs.fetch_add(1, Relaxed);
             Arc::increment_strong_count(manager.store);
             Arc::from_raw(manager.store)
         });
diff --git a/crates/oxidd/src/bdd.rs b/crates/oxidd/src/bdd.rs
index 19d6feb..1630da1 100644
--- a/crates/oxidd/src/bdd.rs
+++ b/crates/oxidd/src/bdd.rs
@@ -38,6 +38,119 @@ pub fn print_stats() {
     oxidd_rules_bdd::simple::print_stats();
 }
 
+#[cfg(all(test, feature = "manager-index", not(feature = "manager-pointer")))]
+mod manager_lifetime_tests {
+    use super::{new_manager, BDDFunction};
+    use crate::{BooleanFunction, Manager, ManagerRef};
+
+    // On Linux, observe the GC thread belonging to each new manager. Checking
+    // its task ID after drop makes asynchronous retirement fail deterministically.
+    #[cfg(target_os = "linux")]
+    fn gc_threads() -> std::collections::HashSet<std::path::PathBuf> {
+        std::fs::read_dir("/proc/self/task")
+            .unwrap()
+            .map(|entry| entry.unwrap().path())
+            .filter(|task| {
+                std::fs::read_to_string(task.join("comm"))
+                    .map(|name| name.trim() == "oxidd mi gc")
+                    .unwrap_or(false)
+            })
+            .collect()
+    }
+
+    #[test]
+    fn repeated_manager_lifetimes_on_one_thread() {
+        for lifetime in 0..1_000 {
+            #[cfg(target_os = "linux")]
+            let previous_gc_threads = gc_threads();
+            let manager = new_manager(4096, 256, 1);
+            let (x, y, z) = manager.with_manager_exclusive(|m| {
+                m.add_named_vars(["x", "y", "z"]).unwrap();
+                (
+                    BDDFunction::var(m, 0).unwrap(),
+                    BDDFunction::var(m, 1).unwrap(),
+                    BDDFunction::var(m, 2).unwrap(),
+                )
+            });
+            for _ in 0..16 {
+                let f = x.and(&y).unwrap().or(&z).unwrap();
+                assert!(f.satisfiable(), "manager lifetime {lifetime}");
+            }
+            #[cfg(target_os = "linux")]
+            let mut new_gc_threads = Vec::new();
+            #[cfg(target_os = "linux")]
+            for _ in 0..1_000 {
+                new_gc_threads = gc_threads()
+                    .difference(&previous_gc_threads)
+                    .cloned()
+                    .collect();
+                if !new_gc_threads.is_empty() {
+                    break;
+                }
+                std::thread::yield_now();
+            }
+            #[cfg(target_os = "linux")]
+            assert_eq!(new_gc_threads.len(), 1, "manager lifetime {lifetime}");
+            drop((x, y, z));
+            drop(manager);
+            #[cfg(target_os = "linux")]
+            {
+                // /proc may briefly retain a task entry after pthread_join.
+                for _ in 0..1_000 {
+                    if !new_gc_threads[0].exists() {
+                        break;
+                    }
+                    std::thread::yield_now();
+                }
+                assert!(
+                    !new_gc_threads[0].exists(),
+                    "GC thread outlived manager lifetime {lifetime}"
+                );
+            }
+        }
+    }
+
+    #[cfg(target_os = "linux")]
+    #[test]
+    fn concurrent_final_refs_retire_gc() {
+        for lifetime in 0..1_000 {
+            let before = gc_threads();
+            let manager = new_manager(1024, 128, 1);
+            let mut gc_task = None;
+            for _ in 0..1_000 {
+                gc_task = gc_threads().difference(&before).next().cloned();
+                if gc_task.is_some() {
+                    break;
+                }
+                std::thread::yield_now();
+            }
+            let gc_task = gc_task.expect("manager GC thread did not start");
+            let other = manager.clone();
+            let barrier = std::sync::Arc::new(std::sync::Barrier::new(3));
+            let first_barrier = barrier.clone();
+            let first = std::thread::spawn(move || {
+                first_barrier.wait();
+                drop(manager);
+            });
+            let second_barrier = barrier.clone();
+            let second = std::thread::spawn(move || {
+                second_barrier.wait();
+                drop(other);
+            });
+            barrier.wait();
+            first.join().unwrap();
+            second.join().unwrap();
+            for _ in 0..1_000 {
+                if !gc_task.exists() {
+                    break;
+                }
+                std::thread::yield_now();
+            }
+            assert!(!gc_task.exists(), "GC thread outlived lifetime {lifetime}");
+        }
+    }
+}
+
 #[cfg(all(feature = "manager-index", not(feature = "manager-pointer")))]
 mod index {
     use oxidd_manager_index::node::fixed_arity::NodeWithLevelCons;
