# A public-API client: builds Spot formulas through the ast_api visitor.
if spot_dep.found()
  tlsf2spot_exe = executable('tlsf2spot', 'tlsf2spot.cpp',
                             dependencies: [tlsf_dep, spot_dep])
endif
